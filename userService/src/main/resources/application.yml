spring:
  application:
    name: userService

  datasource:
    url: jdbc:postgresql://localhost:5432/fitness-user
    username: hasnain
    password: 8513097807hasnain


  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
